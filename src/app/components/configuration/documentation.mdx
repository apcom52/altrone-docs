<BluePrint title="Configuration" description="This component is used for passing configuration settings to nested components" />

<Text.Section>
  # Usage

  `<Configuration>` component is used to override some properties (like classNames or styles) for all
  nested components. You can use one Configuration wrapper inside of another Configuration wrapper - all configuration objects
  will be merged.
</Text.Section>

<Text.Section>
  # Configuration properties

  Use <Text.Keyboard>⌘+F</Text.Keyboard> (or <Text.Keyboard>Ctrl+F</Text.Keyboard>) to find necessary component configurations.
</Text.Section>