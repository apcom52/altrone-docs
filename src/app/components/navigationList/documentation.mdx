import {PropertiesTable} from "@/components/PropertiesTable";
import {ConfigTable} from "@/components/PropertiesTable/ConfigTable";
import {Playground} from "@/components/Playground";
import {Icon, NavigationList, Text} from "altrone-ui";
import {
  NavigationListGroupActionProps,
  NavigationListGroupProps,
  NavigationListLinkProps,
  NavigationListProps
} from "@/app/components/navigationList/properties";
import {NavigationListCSSVariables} from "@/app/components/navigationList/variables";
import {CSSVariablesList} from "@/components/CSSVariablesList";
import {NewLabel} from "@/components/NewLabel";

<BluePrint title="NavigationList" description="This component is used to render a navigation throughout your application" />

<Text.Section gap='xl'>
  # Usage

  <Playground code={`import { NavigationList, Icon } from "altrone-ui";

<NavigationList>
  <NavigationList.Link icon={<Icon i="home" />} label="Home" />
  <NavigationList.Link icon={<Icon i="search" />} label="Explore" />
  <NavigationList.Link icon={<Icon i="notifications" />} label="Notifications" />
  <NavigationList.Link icon={<Icon i="email" />} label="Messages" />
  <NavigationList.Link icon={<Icon i="bookmark_border" />} label="Bookmarks" />
  <NavigationList.Link icon={<Icon i="account_circle" />} label="Profile" />
  <NavigationList.Link label="More" />
</NavigationList>
`}>
      <NavigationList>
        <NavigationList.Link icon={<Icon i="home" />} label="Home" />
        <NavigationList.Link icon={<Icon i="search" />} label="Explore" />
        <NavigationList.Link icon={<Icon i="notifications" />} label="Notifications" />
        <NavigationList.Link icon={<Icon i="email" />} label="Messages" />
        <NavigationList.Link icon={<Icon i="bookmark_border" />} label="Bookmarks" />
        <NavigationList.Link icon={<Icon i="account_circle" />} label="Profile" />
        <NavigationList.Link label="More" />
      </NavigationList>
    </Playground>
</Text.Section>

<Text.Section gap='xl'>
  # Properties
  <PropertiesTable title="Properties" data={NavigationListProps} />
</Text.Section>

<Text.Section gap='xl'>
  # Subcomponents
  This component has some subcomponents - to render a single link, group of them or render group action.

  <Text.Section gap="l">
    ## Group
    <PropertiesTable title="Properties" data={NavigationListGroupProps} />
  </Text.Section>

  <Text.Section gap="l">
    ## GroupAction

    <NewLabel version={31} />

    <PropertiesTable title="Properties" data={NavigationListGroupActionProps} />
  </Text.Section>

  <Text.Section gap="l">
    ## Link
    <PropertiesTable title="Properties" data={NavigationListLinkProps} />
  </Text.Section>
</Text.Section>

<Text.Section gap='xl'>
  # CSS Variables
  <CSSVariablesList data={NavigationListCSSVariables} />
</Text.Section>

<Text.Section gap="xl">
  # Configuration
  This component has some configuration settings. Use `<Configuration>` component to override component configuration.
  <ConfigTable filter="navigationList." />
</Text.Section>