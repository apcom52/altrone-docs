import {ComponentDescription} from "@/types/ComponentProperties";

export const CloseButtonProps: ComponentDescription = {
  extends: 'React.ButtonHTMLAttributes<HTMLButtonElement>',
  properties: [
  ]
}