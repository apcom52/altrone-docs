import {CodeBlock} from "@/components/CodeBlock";

<BluePrint title="Size" />

<Text.Section gap="xl">
  <Text.Section gap="l">
  # Usage

  This TypeScript types is used to define control size. By default all components use `m` size.

  <CodeBlock children={`type Size = 's' | 'm' | 'l';`} />
  </Text.Section>
</Text.Section>