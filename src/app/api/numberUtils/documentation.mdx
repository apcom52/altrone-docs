import {CodeBlock} from "@/components/CodeBlock";

<BluePrint title="NumberUtils" />

<Text.Section gap="xl">
  <Text.Section gap="l">
  # Usage

  Available only in Altrone 3.3 and later. This class provides a set of static methods to work with numbers. You don't need to create an instance of this class.

  <CodeBlock children={`import { NumberUtils } from "altrone-ui"`} />
  </Text.Section>

  <Text.Section gap="l">
    # Methods

    <CodeBlock children={`static function getInRange(value: number, options?: { min?: number, max?: number }) => number`} />
    Returns a number in range from `min` to `max`.
  </Text.Section>
</Text.Section>