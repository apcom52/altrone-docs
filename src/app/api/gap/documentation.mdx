import {CodeBlock} from "@/components/CodeBlock";

<BluePrint title="Gap" />

<Text.Section gap="xl">
  <Text.Section gap="l">
  # Usage

  This TypeScript types is used to define the size between items.

  <CodeBlock children={`type Gap = 'none' | 'xxs' | 'xs' | 's' | 'm' | 'l' | 'xl' | 'xxl';`} />
  </Text.Section>
</Text.Section>