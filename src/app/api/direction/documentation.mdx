import {CodeBlock} from "@/components/CodeBlock";

<BluePrint title="Direction" />

<Text.Section gap="xl">
  <Text.Section gap="l">
  # Usage

  This TypeScript types is used to define the align of child items in the list.

  <CodeBlock children={`type Align = 'start' | 'center' | 'end' | 'between'`} />
  </Text.Section>
</Text.Section>