import {CodeBlock} from "@/components/CodeBlock";

<BluePrint title="Direction" />

<Text.Section gap="xl">
  <Text.Section gap="l">
  # Usage

  This TypeScript types is used to define the axis of the list.

  <CodeBlock children={`type Direction = 'vertical' | 'horizontal';`} />
  </Text.Section>
</Text.Section>