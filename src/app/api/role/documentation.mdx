import {CodeBlock} from "@/components/CodeBlock";

<BluePrint title="Role" />

<Text.Section gap="xl">
  <Text.Section gap="l">
  # Usage

  This TypeScript types is used to define the role of the component.

  <CodeBlock children={`type Role = 'default' | 'primary' | 'success' | 'warning' | 'danger';`} />
  </Text.Section>
</Text.Section>