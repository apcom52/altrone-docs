import {CodeBlock} from "@/components/CodeBlock";

<BluePrint title="FileUtils" />

<Text.Section gap="xl">
  <Text.Section gap="l">
  # Usage

  This class provides a set of static methods to work with files. You don't need to create an instance of this class.

  <CodeBlock children={`import { FileUtils } from "altrone-ui"`} />
  </Text.Section>

  <Text.Section gap="l">
    # Methods

    <CodeBlock children={`static getFileSize(fileSize: number) => string`} />
    Returns a string representation of the file size (like 16.4MB). `fileSize` argument has to be file size in bytes.
  </Text.Section>
</Text.Section>