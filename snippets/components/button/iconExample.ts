export default `import { Button, Size, Role, ButtonVariant, Icon } from 'altrone-ui';

<Button
  size={Size.large}
  role={Role.danger}
  leftIcon={<Icon i="danger" />}
  variant={ButtonVariant.transparent}
  isIcon
>
  <Icon i='question_outline' />
</Button>`