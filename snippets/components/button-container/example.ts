export default `import { ButtonContainer, Button, Direction, Role } from 'altrone-ui';

<ButtonContainer direction={Direction.vertical}>
  <Button>Reset data</Button>
  <Button>Cancel operation</Button>
  <Button role={Role.success}>Submit</Button>
</ButtonContainer>`;